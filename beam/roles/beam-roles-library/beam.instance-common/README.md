# beam.instance-common

Common tasks for deploying on the instance (host)

## Actions

### deploy

Install common, useful instance packages.

**Example of usage:**

```yamlex
roles:
  - { role: beam.instance-common, action: deploy}
```
